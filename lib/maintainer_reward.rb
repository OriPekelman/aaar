#should be a model
class MaintainerReward
  def initialize (github_user)
    @github_user = github_user
    @rewardkind = nil
  end
    
  def @rewardkinds
    [:poney, :kitten, :poem, :squirrel]
  end
    
end
